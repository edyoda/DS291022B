import re

# data = "Python"
# result = re.findall('\APy',data) # checks if the data starts with 'Py'
# print(result)

# data = "Python is a high level programming language!"
# result = re.findall(r'\bpro',data) # checks if the any word in data that starts with 'pro'
# print(result)

# data = "Python is a high level programming language!"
# result = re.findall(r'\Bvel',data) # checks if the any word in data that ends with 'vel'
# print(result)

# data = "Python is a high level programming language! 123"
# result = re.findall(r'\d',data) # checks if the data contains digits
# print(result)

# data = "Python is a high level programming language! 123"
# result = re.findall(r'\D',data) # checks if the data contains non-digits
# print(result)

# data = "Python is a high level programming language! 123"
# result = re.findall(r'\s',data) # checks if the data contains space
# print(result)

# data = "Python is a high level programming language! 123"
# result = re.findall(r'\S',data) # checks if the data contains non-space
# print(result)

# data = "Python"
# result = re.findall(r'thon\Z',data) # checks if the data ends with 'thon'
# print(result)

# data = "Python"
# result = re.findall('\APy',data) # checks if the data starts with 'Py'
# print(result)

# data = "Python is a high level programming language! 123 _"
# result = re.findall('\w',data) # checks if the data contains A-Z,a-z,0-9,_
# print(result)

# data = "Python is a high level programming language! 123 _ @$#@$#"
# result = re.findall('\W',data) # checks if the data contains characters other than A-Z,a-z,0-9,_
# print(result)